name: Publish Python 🐍 distribution 📦 to PyPI

on:
  workflow_dispatch:
  push:
    branches:
      - main  # Change if using a different default branch

jobs:

  # Job 1: Run examples on macOS, Linux, Windows
  test-examples:
    name: Run examples on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .  # install your local package

      - name: Run examples
        shell: bash
        run: |
          find examples -maxdepth 2 -name "*boost_*.py" -exec python {} \;

  # Job 2: Build and publish to PyPI (Linux only)
  publish:
    name: Build and Publish to PyPI
    runs-on: ubuntu-latest
    needs: test-examples  # ensure tests/examples pass first

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build distribution
        run: python -m build

      - name: Check distribution
        run: twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_GLOBAL_SYNTHE }}
          repository-url: https://upload.pypi.org/legacy/
